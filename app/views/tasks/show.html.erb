<h2><%= @task.title %></h2>
<p><%= @task.completed %></p>
<p><%= link_to "Return to the list of tasks", tasks_path %></p>
<p><%= link_to "EDIT", edit_path(@task) %></p>
<p><%= link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %></p>
